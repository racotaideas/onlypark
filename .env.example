# Copiar a .env.local (NO commitear .env.local)
# Estos valores se inyectan en el frontend — usar SOLO el anon key.
# El service_role NUNCA va al bundle del navegador; sólo en env vars de Netlify o Edge Functions.
VITE_SUPABASE_URL=https://ixumzgorhuhftasgrmhg.supabase.co
VITE_SUPABASE_ANON_KEY=pega_aqui_el_anon_key
